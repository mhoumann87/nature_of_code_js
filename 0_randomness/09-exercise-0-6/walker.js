/*
  A walker which has its steps size based on a qualifying random value.
  We adjust the algorithm from the previous lesson. Here we use a squared first number, and if it is smaller than the second random number, the walker will take 5 steps instead of one.
  */

  const acceptReject = () => {
    while (true) {
      // Pick the first random number
      const r1 = random();
      // square the number and use that as the
      const 
    }
  }

