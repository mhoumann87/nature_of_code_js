// A class for the walker
class Walker {
  constructor() {
    // vector for position
    this.position = createVector(width / 2, height / 2);
    // empty vector for velocity
    this.velocity = createVector();
    
  }
}